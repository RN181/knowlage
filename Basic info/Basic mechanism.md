when I type Google.com and hit the enter:

```
- Client <-> name resolution <-> DNS <-> DNS server <-> NS --> IP
- Client <-> TCP/Ip (3-way handshake) <-> SSL handshake
- Client <-> Certificate checking by Browser (iCA)
- Client <-> HTTP <-> IP (web server, CDN, reverse proxy)
  - Web Application( dynamic response)
    -input processing (header, body, query string, etc)
    -Authentication
    -Authorixation
    -SQL Query
    -ETC
- Client <-> server response (HTML)



